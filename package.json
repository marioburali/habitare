{
  "name": "habitare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names BACK,FRONT --prefix-colors green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm i && npm i --prefix backend && npm i --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
